\subsubsection{Finding Related Web Pages for Document ``<{title}>''}
\label{<{subsubsec-id}>}

Table~\ref{<{top-words-id}>} lists the top 5 words.
\begin{table}[H]
\centering
\begin{tabular}{|l|c|}
\hline\hline
    Word & tf-idf \\
\hline<%for word in words%>
    <{word.word}> & <{word.tf-idf|double-format:2}> \\<%endfor%>
\hline\hline
\end{tabular}
\caption{Top 5 words from the document ``<{title}>'' in the \textit{<{collection}>} document set.}
\label{<{top-words-id}>}
\end{table}

The search results based on the query ``<{query}>'':

\begin{enumerate}<%for page in pages%>
\item
    \verb|<{page.title|safe}>| \\
    \url{<{page.link|safe}>}<%endfor%>
\end{enumerate}

Table~\ref{<{ratings-id}>} shows the evaluation of each link.
\begin{table}[H]
\centering
\begin{tabular}{|l|l|l|}
\hline\hline
    \# & Rating (by Author) & Rating (by Committee) \\
\hline
    1 & 0 -- Not useful & ? \\
    2 & 0 -- Not useful & ? \\
    3 & 0 -- Not useful & ? \\
    4 & 0 -- Not useful & ? \\
    5 & 0 -- Not useful & ? \\
\hline
    Average & 0 & ? \\
\hline\hline
\end{tabular}
\caption{Ratings of the links for the document ``<{title}>'' in the \textit{<{collection}>} document set.}
\label{<{ratings-id}>}
\end{table}
